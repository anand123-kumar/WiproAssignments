import React from "react";

// Define a functional component named About
const About = () => {
  // The component returns JSX (UI structure)
  return (
    // A div wrapper that contains all the elements of this component
    <div>
      {/* Heading for the About page */}
      <h2>About</h2>
      {/* Paragraph text describing the page */}
      <p>About the Income Tax Department</p>
    </div>
  );
};

// Export the About component as the default export
export default About;
