import React from "react";

// Define a functional component named AddTaxpayer
const AddTaxpayer = () => {
  // The component returns JSX (UI to render on screen)
  return (
    // A div wrapper that contains all elements of this component
    <div>
      {/* Heading for the page */}
      <h2>Add Taxpayer</h2>
      {/* Paragraph text describing the page */}
      <p>Add Taxpayer Page</p>
    </div>
  );
};

// Export the AddTaxpayer component as the default export
export default AddTaxpayer;
