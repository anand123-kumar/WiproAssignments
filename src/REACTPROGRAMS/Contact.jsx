import React from "react";

// Define a functional component named Contact
const Contact = () => {
  // The component returns JSX (UI structure)
  return (
    // Root <div> element that contains all the Contact page content
    <div>
      {/* Heading element for the Contact section */}
      <h2>Contact</h2>

      {/* Paragraph providing contact information */}
      <p>Contact the Income Tax Department</p>
    </div>
  );
};

// Export the Contact component so it can be imported and used elsewhere
export default Contact;