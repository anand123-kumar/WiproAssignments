import React from "react";

// Defining a functional component called "Home"
const Home = () => {
  return (
    // JSX code starts here: a <div> acts as the root container
    <div>
      {/* Heading for the Home page */}
      <h2>Home</h2>
      
      {/* Paragraph text displaying a welcome message */}
      <p>Welcome to the Income Tax Department Portal</p>
    </div>
  );
};

// Exporting the Home component so it can be used in other files
export default Home;
