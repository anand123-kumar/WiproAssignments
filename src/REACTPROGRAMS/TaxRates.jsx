import React from "react";

// Define a functional component named TaxRates
const TaxRates = () => {
  // Return the JSX (UI structure) that this component will render
  return (
    // Parent container element
    <div>
      {/* Heading for the Tax Rates page */}
      <h2>Tax Rates</h2>

      {/* Paragraph describing the content */}
      <p>Tax Rates Information</p>
    </div>
  );
};

// Export the TaxRates component so it can be imported and used in other files
export default TaxRates;
