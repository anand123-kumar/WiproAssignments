import React from "react";

// Define a functional component named TaxpayerList
const TaxpayerList = () => {
  // The component returns JSX to render UI
  return (
    // Main container div
    <div>
      {/* Heading for the page */}
      <h2>Taxpayer List</h2>

      {/* Paragraph describing the page */}
      <p>Taxpayer List Page</p>
    </div>
  );
};

// Export the TaxpayerList component so it can be used in other files
export default TaxpayerList;
